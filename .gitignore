# build artifacts (root-anchored! a bare "ocgate" pattern would also
# ignore the cmd/ocgate/ source directory)
/ocgate
/ocgate.exe
*.test

# local config
config.yml

# logs
*.log

# smoke build artifact
scripts/smoke/ocgate-smoke
